export enum AppRole {
  Superuser = 'realm:superuser'
}

export enum OperatorTenantStatus {
  Pending = 'pending',
  Active = 'active'
}

export enum TenantRole {
  Admin = 'tenant:admin',
  User = 'tenant:user'
}

export enum OperatorRole {
  Admin = 'operator:admin',
  User = 'operator:user'
}
